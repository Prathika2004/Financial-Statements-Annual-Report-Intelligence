# Copy to .env and fill in as needed. Every value here is optional --
# the pipeline runs entirely without a .env file at all.

# Requires "Authorization: Bearer <value>" on /ask and /ask/stream when set.
# Leave unset for local single-user use (the default) -- see
# config/settings.py's API_KEY discussion for why this is optional here.
SEC_RAG_API_KEY=
