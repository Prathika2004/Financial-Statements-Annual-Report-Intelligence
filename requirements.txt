docling
pydantic-settings
python-dotenv
sentence-transformers
optimum-intel[openvino]   # CPU-optimized embedding inference backend (Stage 5), Intel-specific
optimum[onnxruntime]      # ONNX Runtime fallback backend if OpenVINO is unavailable
qdrant-client
rank-bm25
flashrank
fastapi
uvicorn
streamlit                # UI: chat interface calling the FastAPI backend, see ui/streamlit_app.py
ragas
tqdm==4.66.5   
pdfplumber==0.11.4       # fallback / cross-validation for table extraction
pymupdf==1.24.13   
requests==2.32.3 
pytest==8.3.3
pydantic>=2.11,<3